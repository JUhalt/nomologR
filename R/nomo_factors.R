#' Evaluate evidence about the number of latent factors
#'
#' Planned evidence includes parallel analysis and complementary factor-retention
#' diagnostics using correlations appropriate to the indicator type.
#'
#' @param data A data frame containing candidate items.
#' @param items Optional character vector identifying item columns.
#' @param guidance Guidance settings from [nomo_defaults()].
#'
#' @return During the development series this function is a documented API stub.
#' @export
nomo_factors <- function(data, items = NULL, guidance = nomo_defaults()) {
  nomo_not_implemented("nomo_factors", "Milestone 2")
}
