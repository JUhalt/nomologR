#' Convergent and discriminant construct-validity evidence
#'
#' Planned summaries include standardized loadings, AVE, latent correlations,
#' HTMT/HTMT2, and optional legacy Fornell-Larcker information. No single index
#' will be treated as a declaration that a construct is valid or invalid.
#'
#' @param fit A fitted measurement model.
#' @param guidance Guidance settings from [nomo_defaults()].
#'
#' @return During the development series this function is a documented API stub.
#' @export
nomo_validity <- function(fit, guidance = nomo_defaults()) {
  nomo_not_implemented("nomo_validity", "Milestone 5")
}
