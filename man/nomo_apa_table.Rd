% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nomo_apa.R
\name{nomo_apa_table}
\alias{nomo_apa_table}
\title{Manuscript-ready tables in APA style}
\usage{
nomo_apa_table(x, type = NULL, number = NULL, title = NULL, ...)
}
\arguments{
\item{x}{A result object: \code{nomo_cfa}, \code{nomo_reliability}, \code{nomo_invariance},
or \code{nomo_network}.}

\item{type}{Which table to build. For \code{nomo_cfa}: \code{"loadings"}, \code{"fit"}, or
\code{"factor_correlations"}. For \code{nomo_network}: \code{"hypotheses"} or \code{"fit"}.
Other objects have one table each.}

\item{number}{Optional table number, printed in bold as "Table 1".}

\item{title}{Optional title; a descriptive default is supplied.}

\item{...}{Unused.}
}
\value{
A \code{nomo_apa_table} object, which prints in the console and renders
as a formatted table, with its title and notes, when knitted.
}
\description{
Formats the evidence in a \code{nomologR} result as a table ready for a thesis,
dissertation, or manuscript, following APA 7 conventions: a bold table
number, an italic title, no vertical rules, and notes below the table in the
order general, specific, probability.
}
\details{
\strong{Leading zeros follow the statistic, not the value.} APA 7 drops the
leading zero only for statistics that \emph{cannot} exceed 1. So \emph{p} values,
correlations, reliability coefficients, and CFI are written \code{.95}, while TLI,
RMSEA, SRMR, and standardized loadings keep it, \code{0.95}, because each can
exceed 1 in principle: TLI is not bounded above, and a standardized loading
does in an improper (Heywood) solution. Many published tables print
standardized loadings without the zero; this follows the rule as written.

\strong{No verdicts.} Table notes keep the package's reference-value language.
No cell reads PASS or FAIL, and fit indices are not labeled good or poor.

The rules come from the \emph{Publication Manual of the American Psychological
Association} (7th ed.), checked against Purdue OWL's APA 7 guides.
Journal-specific templates are out of scope.
}
\references{
American Psychological Association. (2020). \emph{Publication manual of the
American Psychological Association} (7th ed.).
\doi{10.1037/0000165-000}
}
