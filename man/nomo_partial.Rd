% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nomo_partial.R
\name{nomo_partial}
\alias{nomo_partial}
\title{Specify researcher-controlled partial-invariance releases}
\usage{
nomo_partial(level, syntax, rationale)
}
\arguments{
\item{level}{Character vector naming the first invariance level at which each
release should apply. Supported labels are \code{thresholds}, \code{metric}, \code{scalar},
\code{strong}, and \code{strict}; the applicable subset depends on the indicator
category structure.}

\item{syntax}{Character vector of lavaan parameter expressions to exclude from
the corresponding equality constraints.}

\item{rationale}{Character vector documenting why each release was chosen.
A single rationale may be recycled across multiple releases.}
}
\value{
A \code{nomo_partial} object with a report-ready release table.
}
\description{
\code{nomo_partial()} records equality constraints that the researcher has decided
to release at a specific invariance level. It does not inspect modification
indices, search for a better-fitting model, or choose releases automatically.
}
\details{
Releases are written using ordinary lavaan parameter syntax accepted by the
\code{group.partial} argument of \code{semTools::measEq.syntax()}, for example
\code{"F =~ x2"}, \code{"x3 ~ 1"}, or \code{"u2 | t1"}.

A release first requested at one level is carried forward to later,
more-restrictive levels so that a loading freed at the metric level does not
silently become constrained again at the scalar level.
}
\examples{
partial <- nomo_partial(
  level = c("metric", "scalar"),
  syntax = c("F =~ x2", "x3 ~ 1"),
  rationale = c(
    "Loading difference was theoretically anticipated.",
    "Intercept difference was prespecified from prior evidence."
  )
)
partial
}
