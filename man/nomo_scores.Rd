% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nomo_scores_guidance.R
\name{nomo_scores}
\alias{nomo_scores}
\title{Score a measurement model, with the evidence for the scoring choice}
\usage{
nomo_scores(
  fit,
  method = c("sum", "mean", "regression", "bartlett"),
  guidance = nomo_defaults()
)
}
\arguments{
\item{fit}{A \code{nomo_cfa} object or fitted \code{lavaan} measurement model.
Single-group, single-level, with no regressions among latent variables.}

\item{method}{Scoring method. \code{"sum"} and \code{"mean"} are unit weighted;
\code{"regression"} and \code{"bartlett"} are model weighted.}

\item{guidance}{A \code{nomo_guidance} object from \code{\link[=nomo_defaults]{nomo_defaults()}}.}
}
\value{
An object of class \code{nomo_scores}: the \code{scores} themselves, a
\code{diagnostics} table of Grice's three criteria per factor, the
\code{unit_weighting} evidence, \code{notes}, and the fitted model.
}
\description{
Computes scores from a fitted measurement model and reports what those
scores are and are not. Scoring is a modeling decision, and \code{nomo_scores()}
documents the decision rather than making it.
}
\details{
\strong{Unit weighting is a model.} McNeish and Wolf (2020) show that adding
items is not a model-free arithmetic calculation but a \emph{parallel} factor
model, assuming equal unstandardized loadings and equal residual variances.
For \code{method = "sum"} and \code{method = "mean"}, that constrained model is fitted
and compared with the model supplied, so a researcher can see whether the
assumption their sum score makes is consistent with their data.

\strong{A score is not the latent variable.} Grice (2001) evaluates factor scores
on three criteria, all reported here and all computed from the fitted model:
\itemize{
\item \code{validity}, the correlation between a score and the factor it estimates.
For \code{method = "regression"} this equals the factor determinacy coefficient
reported by \code{\link[=nomo_hierarchical]{nomo_hierarchical()}}, because that method maximizes it.
\item \code{univocality}, its correlation with the factors it does not represent.
\item \code{correlational_accuracy}, how far correlations among scores sit from the
correlations among the factors they stand in for.
}

The third deserves attention before scores are used in later analyses. A
relationship estimated from scores carries that discrepancy as bias, and its
direction depends on the scoring method and the model rather than being a
constant that can be corrected for. Where a question can be asked of the
latent variables instead, asking it of scores replaces an unbiased answer
with a biased one.

\strong{Thresholds are context.} Gorsuch's (1983) recommendation that validity
reach .80, and above .90 for scores serving as substitutes for the factors
themselves, is reported where a value falls below it and is never applied as
a rule.

The supplied data is never modified, and scores are computed only for the
cases the model used.
}
\references{
Gorsuch, R. L. (1983). \emph{Factor analysis} (2nd ed.). Lawrence Erlbaum.

Grice, J. W. (2001). Computing and evaluating factor scores.
\emph{Psychological Methods, 6}(4), 430-450.
\doi{10.1037/1082-989X.6.4.430}

McNeish, D., & Wolf, M. G. (2020). Thinking twice about sum scores.
\emph{Behavior Research Methods, 52}(6), 2287-2305.
\doi{10.3758/s13428-020-01398-0}
}
\seealso{
\code{\link[=nomo_hierarchical]{nomo_hierarchical()}} for factor determinacy and construct
replicability.
}
