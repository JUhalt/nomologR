% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nomo_split.R
\name{nomo_split}
\alias{nomo_split}
\title{Create a reproducible calibration/validation split}
\usage{
nomo_split(
  data,
  validation_prop = 0.5,
  seed = 1234L,
  guidance = nomo_defaults()
)
}
\arguments{
\item{data}{A non-empty data frame.}

\item{validation_prop}{Proportion of rows assigned to the validation sample.
Must be strictly between 0 and 1.}

\item{seed}{Integer seed used to make the split reproducible.}

\item{guidance}{Guidance settings from \code{\link[=nomo_defaults]{nomo_defaults()}}.}
}
\value{
A \code{nomo_split} object containing \code{calibration}, \code{validation}, a
row-level \code{assignment} table, split sizes, the seed, and a decision log.
}
\description{
\code{nomo_split()} creates an explicit random split for workflows in which EFA
and CFA should be evaluated on different observations when the available
sample permits it. The function does not claim that sample splitting is
always preferable: dividing a modest sample reduces precision in both
subsets, and an external validation sample is generally stronger evidence
when one is available.
}
\details{
The caller's random-number-generator state is restored after the split so
that using \code{nomo_split()} does not silently alter later stochastic analyses.
}
\examples{
split <- nomo_split(nomo_demo_continuous, validation_prop = 0.40, seed = 2026)
split
nrow(split$calibration)
nrow(split$validation)
}
\references{
Fokkema, M., & Greiff, S. (2017). How performing PCA and CFA on the same
data equals trouble. \emph{European Journal of Psychological Assessment, 33}(6),
399-402. \doi{10.1027/1015-5759/a000460}

MacCallum, R. C., Roznowski, M., & Necowitz, L. B. (1992). Model
modifications in covariance structure analysis: The problem of
capitalization on chance. \emph{Psychological Bulletin, 111}(3), 490-504.
\doi{10.1037/0033-2909.111.3.490}
}
