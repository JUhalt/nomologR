# Plot measurement-invariance evidence

Plot measurement-invariance evidence

## Usage

``` r
# S3 method for class 'nomo_invariance'
plot(x, type = c("fit", "change", "local_strain"), ...)
```

## Arguments

- x:

  A `nomo_invariance` object.

- type:

  Plot type: `"fit"`, `"change"`, or `"local_strain"`.

- ...:

  Unused.

## Value

A `ggplot2` object.
