# Print a factor-retention summary

Print a factor-retention summary

## Usage

``` r
# S3 method for class 'summary_nomo_factors'
print(x, ...)
```

## Arguments

- x:

  A `summary_nomo_factors` object.

- ...:

  Additional arguments, currently ignored.

## Value

`x`, invisibly.
